PORT=3001
JWT_SECRET=your-secret-key-change-this
JWT_REFRESH_SECRET=your-refresh-secret-change-this

# Bitcoin Testnet (optional - leave empty for simulated addresses)
BTC_MASTER_MNEMONIC=
BTC_NETWORK=testnet

# Email (optional - set EMAIL_ENABLED=true when configured)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=noreply@myescrow.com
EMAIL_ENABLED=false

# Deposit monitoring
DEPOSIT_POLL_INTERVAL=30000
DEPOSIT_MIN_CONFIRMATIONS=1

# Admin
ADMIN_EMAIL=admin@demo.com
